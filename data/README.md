# Data

The raw and processed datasets are not uploaded to this public repository because of file-size and data-sharing considerations.

The datasets are stored separately in a shared drive and can be accessed by project members only.

Required files:
- filtered_tcga_expression_mean.csv
- filtered_addition_expression_mean.csv
- tcga_meta.csv
- GSE72094_meta.csv
